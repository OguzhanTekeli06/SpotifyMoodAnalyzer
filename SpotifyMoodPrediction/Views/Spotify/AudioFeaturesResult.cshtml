@{
    ViewBag.Title = "Audio Features";
}

<h2>Audio Features</h2>

@if (ViewBag.AudioFeatures != null)
{
    <pre>@Html.Raw(ViewBag.AudioFeatures)</pre>
}
else
{
    <p>No audio features available.</p>
}
